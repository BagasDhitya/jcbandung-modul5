name: Low Stock Scheduler

on:
  schedule:
    - cron: "* * * * *" # setiap 1 menit

jobs:
  hit-api:
    runs-on: ubuntu-latest

    steps:
      - name: Call API
        run: |
          curl -X GET "https://warungkuu-api.vercel.app/jobs/low-stock"
